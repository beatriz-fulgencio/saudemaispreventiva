# Backend Configuration
HOST=0.0.0.0
PORT=8000

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Ollama Configuration (Optional - for enhanced AI responses with local model)
# If Ollama is running on host macOS: http://host.docker.internal:11434
# If Ollama is running in Docker: http://ollama:11434
# Leave empty to use only ML-based analysis
OLLAMA_HOST=http://host.docker.internal:11434

# Database (future)
# DATABASE_URL=sqlite:///./saude_preventiva.db
